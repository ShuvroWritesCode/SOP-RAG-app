import { OnUnauthorizedExceptionFilter } from './on-unauthorized-exception.filter';

describe('OnUnauthorizedExceptionFilter', () => {
  it('should be defined', () => {
    expect(new OnUnauthorizedExceptionFilter()).toBeDefined();
  });
});
